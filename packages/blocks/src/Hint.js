import React from 'react'
import PropTypes from 'prop-types'

function Hint({...props}) {
  return (
    <div>
      <p>Hint stateless</p>
    </div>
  )
}

Hint.propTypes = {}

Hint.defaultProps = {}

export default Hint
